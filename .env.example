# Required for the chat agent
ANTHROPIC_API_KEY=

# Required for manual vector retrieval (run npm run index:manual after setting)
OPENAI_API_KEY=

# Set to 1 to skip the Manager review pass (faster, less validation)
# DISABLE_MANAGER=

# Voice agent (optional -- text mode works without these)
ELEVENLABS_API_KEY=
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=

# Extraction scripts only (not needed for runtime)
OPENAI_API_KEY=
GOOGLE_AI_API_KEY=
